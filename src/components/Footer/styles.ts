import styled from "styled-components";

export const FooterStyles = styled.footer`
  margin: 16px 0;

  p {
    text-align: center;
  }

  a {
    text-decoration: none;
    cursor: pointer;
    font-weight: bold;
  }
`

import styled from "styled-components";

export const FooterStyles = styled.footer`
  margin-top: 16px;

  p {
    text-align: center;
  }

  a {
    text-decoration: none;
    cursor: pointer;
    font-weight: bold;
  }
`
